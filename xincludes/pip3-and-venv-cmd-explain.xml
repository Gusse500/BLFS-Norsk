<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect3 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
  ]>

  <sect3 role="commands">
    <title>Parameterforklaringer</title>

    <para>
      <parameter>-w dist</parameter>: bygger den passende
      "wheel" for denne modulen i mappen
      <filename class="directory">dist</filename>.
    </para>

    <para>
      <parameter>--no-build-isolation</parameter>: forteller pip3 at den skal kjøre 
	  byggingen i systemmiljøet i stedet for å opprette et midlertidig 
	  byggemiljø.
    </para>

    <para>
      <parameter>--no-deps</parameter>: hindrer pip3 i å bygge wheels for
      prosjektets avhengigheter.
    </para>

    <para>
      <parameter>--no-index</parameter>: ignorerer pakkeindeksen 
	  (ser bare på --find-links URL-er i stedet)).
    </para>

    <para>
      <parameter>--find-links dist</parameter>: ser etter lenker til arkiver
      som wheel (.whl) filer i mappen
      <filename class="directory">dist</filename>.
    </para>

    <para>
      <parameter>--no-cache-dir</parameter>: Deaktiverer hurtigbufferen for å forhindre 
	  en advarsel ved installasjon som &root; bruker.
    </para>

    <para>
      <parameter>--no-user</parameter>: Forhindre at du feilaktig kjører 
	  install kommandoen som ikke-root bruker.
    </para>

    <para>
      <parameter>--system-site-packages</parameter>: Tillater at
      <application>Python3</application> venv modulen får tilgang til
      systeminstallerte <filename
      class="directory">/usr/lib/python&python3-majorver;/site-packages</filename>
      mappen.
    </para>

    <para>
      <option>--upgrade</option>: Oppgrader pakken til den nyeste 
	  tilgjengelige versjonen. Dette alternativet brukes med install kommandoen 
	  hvis en versjon av pakken allerede er installert..
    </para>

    <para>
      <option>--force-reinstall</option>: Installer pakken på nytt selv om 
	  den er oppdatert. Dette alternativet brukes med install kommandoen 
	  hvis du installerer pakken på nytt eller går tilbake til en tidligere versjon 
	  av pakken.
    </para>

    <para>
      <option>--no-deps</option>: Ikke installer pakkeavhengigheter. 
	  Dette alternativet kan være nødvendig med --upgrade eller --force-reinstall
      alternativene.
    </para>

  </sect3>

